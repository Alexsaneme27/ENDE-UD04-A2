package figuras;
import java.awt.Color;
public class Cuadrado extends Rectangulo{
    //Añado comentario para la rama develop
    public Cuadrado (double x, double y, Color color, double lado){
        super (x, y, color, lado, lado);
    }
}